node_modules/
.next/
.env*.local
.vercel
*.log

.vercel
.env*
# ...except the template, which carries names only and never values.
!.env.example
.signing-key.txt
tsconfig.tsbuildinfo
.next-dev.log
.next-prod.log

# demo captures: large, regenerated by scripts/record-demo.mjs
work/

# scaffolded duplicate of AGENTS.md; this repo carries no vendor-agent files
videos/**/CLAUDE.md
videos/**/snapshots*/
videos/**/renders/
videos/**/assets/*.webm

# holds no secrets but is machine-local: paths and the env it sources
scripts/.arena-start.local.sh
scripts/.arena1-start.local.sh
scripts/.arena2-start.local.sh
